package com.mycity.place.controller;

import org.springframework.stereotype.Controller;

@Controller
public class PlaceController {
		
//	- `getAllPlaces(Pageable pageable, @RequestParam(required = false) String category, @RequestParam(required = false) String searchQuery)` : Retrieves a paginated list of places with optional category filtering and search.
//	- `getPlaceById(@PathVariable Long placeId)` : Retrieves a specific place by ID.
//	- `getPlacesByCategory(@PathVariable String category)` : Retrieves places within a specific category.
//	- `searchPlaces(@RequestParam String query)` : Searches for places based on a search query.
//	- `getNearbyPlaces(@RequestParam double latitude, @RequestParam double longitude, @RequestParam double radius)` : Retrieves places within a specified radius of a location.
//	- `getTopRatedPlaces()` : Retrieves the top-rated places.
//	- `getPopularPlaces()` : Retrieves popular places.
//	- `getFeaturedPlaces()` : Retrieves featured places.
//	- `getPlaceReviews(@PathVariable Long placeId, Pageable pageable)` : retrieves the reviews of a place.
//	- `getPlaceImages(@PathVariable Long placeId)` : retrieves the images of a place.
//	- `getPlaceEvents(@PathVariable Long placeId, Pageable pageable)` : retrieves the events happening at a place.
//	- `getPlaceListings(@PathVariable Long placeId, Pageable pageable)` : retrieves the

	
}
