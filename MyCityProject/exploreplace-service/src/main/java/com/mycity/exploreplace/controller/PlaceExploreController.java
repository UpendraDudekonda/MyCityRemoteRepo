package com.mycity.exploreplace.controller;

import org.springframework.stereotype.Controller;


@Controller
public class PlaceExploreController {

	
	//getParticularPlaceDetails()
	
//	- `getPlacesByUserId(@PathVariable Long userId)` : Retrieves places created by a specific user.
	
//	- `getTop10Places()` : Retrieves the top 10 places.
//	- `getTop10Places(Pageable pageable)` : Retrieves the top 10 places with pagination.
//	- `getTop10Places(@RequestParam(required = false) String category)` : Retrieves the top 10 places with optional category filtering.
//	- `getTop10Places(@RequestParam(required = false) String sort)` : Retrieves the top 10 places with optional sorting.
//	- `getTop10Places(@RequestParam(required = false) double latitude, @RequestParam(required = false) double longitude)` : Retrieves the top 10 places, optionally biased by location.
//	- `getNearbyPlaces(@RequestParam double latitude, @RequestParam double longitude, @RequestParam double radius)` : Retrieves places within a specified radius of a location.
//	- `getRecommendedPlaces()` : Retrieves a list of recommended places based on user preferences or system algorithms.
//	- `getCuratedPlaces(@RequestParam String curatorId)` : Retrieves a list of places curated by a specific user or entity.
//	- `getTrendingPlaces()` : Retrieves a list of trending places.
//	- `getPlacesByInterest(@RequestParam String interest)` : Retrieves a list of places based on a user's interest.
//	- `getPlacesByTheme(@RequestParam String theme)` : Retrieves a list of places that fit a specific theme.
//	- `getPlacesByLocation(@RequestParam double latitude, @RequestParam double longitude, @RequestParam double radius)` : Retrieves a list of places within a specified radius of a location, specifically for exploration.
//	
//	
//	
	
}
