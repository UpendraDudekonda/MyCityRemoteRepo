package com.mycity.admin.controller;

public class AdminUserBookingController {
		
//	`getBookingById(@PathVariable Long bookingId)` : Retrieves details of a specific booking.
//
//	`getBookingsByUser(@PathVariable Long userId)` : Retrieves all bookings for a specific user.
//
//	`getBookingsByMerchant(@PathVariable Long merchantId)` : Retrieves all bookings for a specific merchant.
//
//	`getBookingsByDateRange(@RequestParam @DateTimeFormat(iso = DateTimeFormat.ISO.DATE_TIME) LocalDateTime startDate, @RequestParam @DateTimeFormat(iso = DateTimeFormat.ISO.DATE_TIME) LocalDateTime endDate)` : 1  Retrieves all bookings within a date range. 


}
