package com.mycity.admin.controller;

public class AdminMerchantVerificationController {
	
	
//	- `getPendingMerchantVerifications(Pageable pageable)` : Retrieves a paginated list of merchants awaiting verification.
//	- `getMerchantVerificationDetails(@PathVariable Long merchantId)` : Retrieves detailed information about a merchant's verification request.
//	- `approveMerchantVerification(@PathVariable Long merchantId)` : Approves a merchant's verification request.
//	- `rejectMerchantVerification(@PathVariable Long merchantId, @RequestBody MerchantRejectionDTO rejectionDTO)` : Rejects a merchant's verification request, with a reason for rejection.
//	- `searchMerchantsForVerification(@RequestParam String searchQuery, Pageable pageable)` : Allows admin to search for merchants that are undergoing verification.
//	- `getVerifiedMerchants(Pageable pageable)` : Retrieves a paginated list of all verified merchants.
//	- `getRejectedMerchants(Pageable pageable)` : Retrieves a paginated list of all rejected merchants.

	
}
